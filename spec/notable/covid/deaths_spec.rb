RSpec.describe Notable::Covid::Deaths do
  it "has a version number" do
    expect(Notable::Covid::Deaths::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
